import Bleed from 'nextra-theme-docs/bleed'
import Callout from 'nextra-theme-docs/callout'

# regular expressions

> examples with python

## 

```python
import re

my_string = "  thi s _ugly _ st ring "

stripped_string = my_string.strip()

prettier_string = re.sub("\s", "", stripped_string)


my_string = re.sub("ugly", "prettier", prettier_string)


result: "this_prettier_string"

```
