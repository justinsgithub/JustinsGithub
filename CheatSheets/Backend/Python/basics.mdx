import Callout from 'nextra-theme-docs/callout'

# python


## read json 

```python
import json

with open('message.json') as message_json:
  message = json.load(message_json)
  print(message['text'])
```

## write json

```python
import json
data_payload = [
  {'request message': 'serve me an article',
   'response message': 'authenticate yourself first'}
]
with open('data.json', 'w') as data_json:
  json.dump(data_payload, data_json)

```

## read a file

```python
with open('my-file.txt') as my_file:
  file_content = my_file.read()
print(file_content)
```

## read lines of a file

```python
with open('my-file.txt') as my_file:
    for line in my_file.readlines():
        print(line)
```

## read file line by line

```python
with open('my-file.txt') as my_file:
    first_line = my_file.readline()
    second_line = my_file.readline()
    print(first_line)
    print(second_line)
```

## write to file

```python
with open('my-file.txt', 'w') as my_file:
    my_file.write('hope this is a new file')
```

## append to file

```python
with open('my-file.txt', 'a') as my_file:
    my_file.write('this is line 2 of my_file.txt')
```
## while loop

```python
# This loop will only run 1 time
hungry = True
while hungry:
  print("Time to eat!")
  hungry = False
 
# This loop will run 5 times
i = 1
while i < 6:
  print(i)
  i = i + 1
```

## for loop

```python
nums = [1,2,3,4,5]
for num in nums: 
  print(num)
```

## dictionaries

```python
my_dictionary = {"sport": "hockey", "position": "center"}
print(my_dictionary["position"])
my_dictionary["position"] = "center"

dict1 = {'color': 'blue', 'shape': 'circle'}
dict2 = {'color': 'red', 'number': 42}
 
dict1.update(dict2)
 
# dict1 is now {'color': 'red', 'shape': 'circle', 'number': 42}

ex_dict = {"a": "anteater", "b": "bumblebee", "c": "cheetah"}
 
ex_dict.keys()
# dict_keys(["a","b","c"])
 
ex_dict.values()
# dict_values(["anteater", "bumblebee", "cheetah"])
 
ex_dict.items()
# dict_items([("a","anteater"),("b","bumblebee"),("c","cheetah")])

# without default
{"name": "Victor"}.get("name")
# returns "Victor"
 
{"name": "Victor"}.get("nickname")
# returns None
 
# with default
{"name": "Victor"}.get("nickname", "nickname is not a key")
# returns "nickname is not a key"
```

## nested loop

```python3

groups = [["Jobs", "Gates"], ["Newton", "Euclid"], ["Einstein", "Feynman"]]
 
# This outer loop will iterate over each list in the groups list
for group in groups:
  # This inner loop will go through each name in each list
  for name in group:
    print(name)

```



## with keyword

> invokes context manager for specified file which takes care of closing file for us after function finishes

```python
no-with-example = open('without-with.txt', 'a')
no-with-example.write('make sure you close this file')
no-with-example.close()
```

## break keyword 

```python

numbers = [0, 254, 2, -1, 3]
 
for num in numbers:
  if (num < 0):
    print("found negative, breaking loop")
    break
  print(num)

print("continuing program, 3 did not get printed")
  
```




## for loop
```python
nums = [1,2,3,4,5]
for num in nums: 
  print(num)
```

## list comprehension

```python

result = [x**2 for x in range(10) if x % 2 == 0]

# translation 
# result is a list containing number to the power of 2 for every number in 0 - 9, but only if number is divisible by 2 ( an even number ) 

print(result)
# [0, 4, 16, 36, 64]

```

## csv file

```python
with open('lots_of_commas.csv') as csv_file:
  print(csv_file.read())
```
## csv library

```plaintext
Name,Username,Email
name one,username1,email@example.com
name two,username2,email2@example.com
name three,username3,email3@example.com
```
> use newline so python does not mistake a line break as a new row of data
```python
import csv
 
list_of_email_addresses = []
with open('the_above_example.csv', newline='') as example_csv:
  user_reader = csv.DictReader(example_csv)
  for row in user_reader:
    list_of_email_addresses.append(row['Email'])
```

## not csv, csv

```plaintext
Name;Address;Telephone
name one;fake address one\n,somecity AA 55555;000-000-0000
name two;fake address two\n,somecity AA 55555;000-000-0000x111
name three;fake address three\n,some AA 55555;000-000-0000x111
```

```python
import csv
with open('addresses.csv', newline='') as addresses_csv:
      address_reader = csv.DictReader(addresses_csv, delimiter=';')
      for row in address_reader:
        print(row['Address'])

```
## add csv coloumn to list

```python
import csv
with open('books.csv') as books_csv:
    books_reader = csv.DictReader(books_csv, delimiter='@')
    isbn_list = []
    for row in books_reader:
        isbn_list.append(row['ISBN'])
```

## write csv to file

```python
big_list = [{'name': 'name one', 'userid': 2525942, 'is_admin': False}, {'name': 'name two', 'userid': 15890235, 'is_admin': False}, {'name': 'name three', 'userid': 572189563, 'is_admin': True}] 
 
import csv
 
with open('output.csv', 'w') as output_csv:
  fields = ['name', 'userid', 'is_admin']
  output_writer = csv.DictWriter(output_csv, fieldnames=fields)
 
  output_writer.writeheader()
  for item in big_list:
    output_writer.writerow(item)
```

## leave a comment

```python
#! /bin/env python3
# This is a comment
```

## output to terminal 

```python
print('this will show on terminal screen')

```

## data types

### string
### int ( whole number )
### float ( decimal number )

## data structures

### list
### dictionary

## operators 

### \+
### \-
### \*
### /
### %
### **

## relational operators

### `==`
### `!=`
### `>`
### `<`
### `>=`
### `>=`

## boolean operators

### and
### or
### not

## square a number

```python
print(2**2)
```

## get the remainder 

```python
print(15%3)
```

## add / concatenate strings

```python
print('hello ' + 'world')

first_word = 'hello'
second_word = 'world'

print ( first_word + second_word )

greeting = first_word

greeting += ' '
subject = second_word

print(greeting + subject)

greeting += (subject + '!')

multi_line_string = '''
I am a
multi line
string
'''
# initials
print('             ')
print(' JJJJJ    A  ')
print('     J    A  ')
print('     J   A A ')
print(' J   J   AAA ')
print('   J    A   A')

```

## use module 
> password is a variable inside of a file called mysecrets.py in our current working directory
```python 

from datetime import datetime 
current_time = datetime.now()
print(current_time)

```
## use personal module 
> password is a variable inside of a file called mysecrets.py in our current working directory
```python 

from mysecrets import password 
print(password)

```
## booleans, control flow, comparison operators

```python
if 13 - 6 != (3 * 2) + 1:
  print('true statement') # shows nothing

if 13 - 6 == (3 * 2) + 1:
  print('true statement') # shows

if 13 - 6 != (3 * 2) + 1:
  print('true statement') # shows nothing
else:
  print('false statement') # shows

if 13 - 6 != (3 * 2) + 1:
  print('false statement') # shows nothing
else:
  print('won\'t print "false statement" because the "if" condition is not true') # shows

if  not(1 + 1  != 10):
    print("1 +1 equals 10")
elif 1 + 1 == 10:
    print("2 equals 10")
elif 10 - 10 + 10 == 10:
    print("True")

True or (3 + 4 == 7)    # True, no matter the second expression if True is the first expression 

(4 == 'four') or True   # True, no matter the first expression if True is the second expression 

(1 - 1 == 0) or False   # True

(1 + 1 == 0) or False   # False, neither statement is True

(2 < 0) or True         # True

(2 > 0) and False         # False, using "and" if any statement is False, the evaluation is false

(3 == 8) or (3 > 4)     # False

13 - 6 != (3 * 2) + 1 # False, 7 IS equal to 7 

13 - 6 == (3 * 2) + 1 # True

(2 + 2 + 2 >= 6) and (-1 * -1 < 0) # False

(2 + 2 + 2 >= 6) or (-1 * -1 < 0) # True

(4 * 2 <= 8) and (7 - 1 == 6) # True

not True == False # True

not False == True # True

not True == True # False

not False == False # False

not (True == False or True == True) # False
```

## lists

```python
list_methods = ['.remove()','.append()','.count()','.insert()','.pop()','.sort()']

list_functions = ['range()','len()','sorted()']

not_a_list = range(9)

number_list = list(range(9))

zero_to_seven = list(range(8))

even_numbers = list(range(0,100,2))

odd_numbers = list(range(1,100,2))

count_by_tens = list(range(10,100,10))

count_by_7 = list(range(3,867,7))

amount_of_fours = count_by_7.count(4)

relational_operators = ['==','!=','>','<','>=','<=']

or_equal = relational_operators[4:]

less_than_or_equal = relational_operators[-n:]

equal_and_not_equal = relational_operators[:2]

greater_and_less_than = relational_operators[2:4]

boolean_operators = ['or','not','not']

operator_names = ['plus', 'minus', 'multiply', 'divide', 'remainder of', 'power of']

operators = ['+', '-', '*', '/', '%', '**']

operator_tuple = ('+', '-', '*', '/', '%', '**')

operators_with_names = list(zip(operator_names, operators))

plus, minus, multiply, divide, remainder_of, power_of = operator_tuple

tuple_single = ('single item',)

boolean_operators.append('and')

all_operators[-2].remove('not')

all_operators = operators + boolean_operators + relational_operators 

all_operators[-1][-1] == '>='

alphabetical_order_booleans = sorted(boolean_operators)

boolean_operators.sort() # ['and','not','or']

boolean_operators.sort(reverse=True) # ['or','not','and']

```
